

<%# Step 2: insert the updated date at the correct position %>

  <%= turbo_stream.replace @exercise do %>
    <%= render @exercise, workout: @workout, day: @day %>
  <% end %>


<%# <%= render_turbo_stream_flash_messages %> 
