<%= render @history_days %>
 <%# save the day.id that should be all need.
 then on save button click.
 history must belong to day to get day index on day show screen.
 then can find day by id.
 can gets it's exercises.
 save history_days to a table with a date
 save exercises to an history_exercise table has a title and day_id
 save sets to a table with set, weight, reps and exercise_id
 then they could be shown on a page. %>
