  <%= turbo_stream_from "workouts" %>
  <div class="flex flex-wrap items-center justify-between mb-6 gap-2 max-[320px]:justify-evenly">
    <h1 class="text-4xl font-bold">Workouts</h1>
    <%= link_to "New workout",
                  new_workout_path,
                  data: { turbo_frame: dom_id(Workout.new) },
                  class: "bg-red-700 hover:bg-red-600 text-white text-lg font-semibold py-3 px-5 rounded-md" %>
  </div>

  <%= turbo_frame_tag Workout.new %>

  <%= turbo_frame_tag "workouts" do %>
    <%= render @workouts %>
  <% end %>
  
