<%= turbo_stream.update nested_dom_id(@exercise, ExerciseSet.new), "" %>

<%# Step 2: append the created line item to the list %>
<%= turbo_stream.append nested_dom_id(@exercise, "exercise_sets") do %>
  <%= render @exercise_set, workout: @workout, day: @day, exercise: @exercise %>
<% end %>

<%# <%= render_turbo_stream_flash_messages %> 