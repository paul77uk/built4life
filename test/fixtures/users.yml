# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
one:
  email: firstemail@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'firstpassword') %>

two:
  email: secondemail@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'secondpassword') %>

three:
  email: thirdemail@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'thirdpassword') %>
